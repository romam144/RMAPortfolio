# RMAPortfolio

Public static portfolio website for Ramichai.

This repository is meant to be connected directly to Vercel. The project root is this folder: `index.html`, `work.html`, `personal.html`, `films.html`, `project.html`, `css`, `js`, `data`, and `uploads` should all stay at the top level.

Private admin files should not be uploaded here.
